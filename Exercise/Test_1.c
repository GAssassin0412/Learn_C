//编写代码实现：使用for循环输出10---30范围内既能被2整除又能被3整除的数（提交代码和运行结果图）
#include <stdio.h>
int main() {
    int i;
    for(i=10;i<31;i++) {    //使i遍历10-30
        if (i % 2 == 0 && i % 3 == 0) {     //既能被2整除又能被3整除时，输出
            printf("%d\n",i);
        }
    }
}